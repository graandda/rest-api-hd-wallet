version: "3"

services:
  app:
    image: graandda/api-wallet
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    environment:
      - DB_HOST=db
      - DB_NAME=ethwallet
      - DB_USER=ethwallet
      - DB_PASSWORD=ethwallet
      - SECRET_KEY=django-insecure-962$l*q&6a(5opvn)@&%f1krrm-@q7g^aw8!d3yii^(k7fdvkt
      - PROVIDER_API_KEY=6fd8ac1ebdb54ae6b31c4e77153e86ab
      - ETHERSCAN_API_KEY=5NCHHSZJ4S4XTP82U6EVGM8PZ85PK4BFYQ

    depends_on:
      - db
  db:
    image: postgres:14-alpine
    environment:
      - POSTGRES_DB=ethwallet
      - POSTGRES_USER=ethwallet
      - POSTGRES_PASSWORD=ethwallet
